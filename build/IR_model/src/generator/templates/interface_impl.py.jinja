"""
{{ module_name }} Interface Implementation

This module implements the {{ interface_name }} interface.
Generated by IR Build System Module Generator.
"""
from typing import Optional, Dict, Any
from ..interfaces.{{ interface_name|snake_case }} import {{ interface_name }}

class {{ module_name }}({{ interface_name }}):
    """Implementation of {{ interface_name }} interface for {{ module_name }}."""
    
    def __init__(self, config: Optional[Dict[str, Any]] = None):
        """Initialize the {{ module_name }} implementation.
        
        Args:
            config: Optional configuration dictionary
        """
        self.config = config or {}
        
    {% for method in interface_methods %}
    def {{ method.name }}({{ method.params|join(', ') }}) {{ method.return_annotation }}:
        """{{ method.docstring }}
        
        {% for param in method.param_docs %}
        Args:
            {{ param.name }}: {{ param.description }}
        {% endfor %}
        
        Returns:
            {{ method.return_doc }}
            
        Raises:
            {{ method.raises|join('\n            ') }}
        """
        raise NotImplementedError("{{ method.name }} not implemented")
    {% endfor %}
